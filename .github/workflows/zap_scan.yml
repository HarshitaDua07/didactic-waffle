name: ZAP Baseline Scan

on:
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.10.0
        with:
          target: 'https://example.com'  # 🔁 Replace with your actual target URL
